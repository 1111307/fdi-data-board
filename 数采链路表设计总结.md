# 数采链路明细/汇总表设计总结(踩坑复盘)

> 本文回答四个问题:①一条数据从车辆上电到上传云端经过哪些环节、落到哪张表;②各明细表的 DDL 与 key 设计思路;③聚合明细表踩过的坑;④聚合表的 key 设计思路。
> 全部结论经过 Doris 生产库实测验证(2026-08,数字为 8/26 分区实测)。

---

## 一、车辆上电后发生了什么(数据产生的时序)

车辆上电、智驾系统启动后,数据按两条独立的流产生:

**流 A:运行心跳流(时间驱动,无 uuid)**
```
上电 → 筛选器进程启动 → 每 5 分钟上报一次心跳(开关状态/版本/心跳状态)
     → fff_running(3.04 亿行/天,30 万辆车)
     → 筛选器被关闭时:fff_close(关闭原因/版本)
```
心跳只是"筛选器活着吗"的监控信号,**不产生录制**,永远不会进入落盘/上传阶段。

**流 B:录制链路流(事件驱动,uuid 串联的漏斗)**

```
事件发生(急刹/碰撞等)                        ← 事件触发录制的那一刻
  │  筛选器判断:cooldown 防抖?配额?     ──┐
  │  触发 → 录制窗口:前 N 秒 + 后 M 秒     │ fff_trigger  3.91 亿触发/天
  │  (before=20s, after=10s)               │ uuid 在这里诞生
  │  其中 3.66 亿被防抖拦截(有效拦截)       ──┘
  ▼
触发成功 2495 万/天 进入落盘
  │  车端写盘:waiting → dumping → 终态     ──┐
  │  每个状态变更发一条消息(重试也是一条)     │ fdr_trigger  2497 万 uuid/天
  │  终态:success / discard / fail          │ 状态快照流:5200 万行/天
  │  (mem pool water line 等原因丢弃)       ──┘
  ▼
落盘成功 → 进入上传(另有 Forever_log 旁路直传,不落盘)
  │  上传:waiting → dumping → 重试 → 终态   ──┐
  │  fcl 平均每 uuid 4 行(重试多)           │ fcl_trigger  1897 万 uuid/天
  │  终态:success / discard(黑名单/配额拦截)──┘
  ▼
上传成功 613 万/天 → fcl_uploadinfo(包级,604 万/天)

观测层:dwd_cfdi_status_monitor_analysis 同步聚合每个 uuid 的三阶段状态
       (fff/fdr/fcl_status 同行) → ads_do_cfdi_daily(uuid 口径计数表)
```

**关键事实(实测)**:
- 上传入口有三个:`trigger_source` = FFF(正路,1358 万)/ Forever_log(旁路直传,539 万,完全不产生 fff/fdr 记录)/ FDC(371,忽略)
- 所以"上传数 > 落盘成功数"不是 bug——旁路贡献了 1/4
- `anonymous_id` 标识车(30 万辆),`uuid` 标识"这辆车这一次触发"(一天 3.9 亿个),uuid 是全链路的 join key

---

## 二、明细表 DDL 与 key 设计思路

### 三种 key 模型先摆清楚

| 模型 | 同 key 多行 | 语义 | 适用 |
|---|---|---|---|
| DUPLICATE | 全保留 | key 只是物理排序前缀,不是约束 | 天然不重复或重复即信息的流水 |
| UNIQUE | 新盖旧(整体替换) | key 是唯一约束,等价于"全列 REPLACE 的 AGGREGATE" | 每实体一行、要覆盖更新 |
| AGGREGATE | 按列合并(SUM/REPLACE...) | key 是分组键 | 预聚合结果表 |

### 各表实测 DDL 摘要

| 表 | 模型 | key | 分桶 | 一行是 |
|---|---|---|---|---|
| fff_trigger | UNIQUE | (dt, uuid, event_name, anonymous_id) | 128桶@anonymous_id | 一次触发(**不含 timestamp,天然 uuid 一行**) |
| fff_running | UNIQUE | (dt, filter_name, anonymous_id, timestamp_utc) | 32桶@anonymous_id | 一次心跳快照 |
| fff_close | UNIQUE | (dt, filter_name, version, reason, anonymous_id) | — | 一次关闭 |
| fdr_trigger | UNIQUE | (dt, uuid, event_name, anonymous_id, **timestamp_utc**) | 128桶@anonymous_id | **一个状态快照** |
| fcl_trigger | UNIQUE | (dt, uuid, event_name, anonymous_id, **timestamp_utc**) | 128桶@anonymous_id | **一个状态快照** |
| fcl_uploadinfo | UNIQUE | (dt, uuid, event_name, anonymous_id) | — | 一个数据包 |
| fdr_bandwidth | UNIQUE | (dt, timestamp_utc, anonymous_id) | — | 一次带宽采样 |
| fdr_fragment | UNIQUE | (dt, uuid, event_name, anonymous_id) | — | 一次碎片率采样 |
| monitor_analysis | UNIQUE | (dt, anonymous_id, event_name, uuid) | 4桶@anonymous_id | 一次触发的全链路状态(**uuid 一行,三阶段同行**) |

### key 设计思路逐条

1. **dt 永远在 key 第一位**——它是分区键,key 前缀与分区对齐,写入和 compaction 都按分区隔离,分区裁剪直接跳过不相关天数(删除过期 = drop 分区,秒级)。

2. **分桶键选 anonymous_id(车辆)**——同车数据物理同桶,车辆维度查询局部性好;分桶数按数据量给(量最大的 trigger 表 128 桶,小表少桶)。桶数即写入/查询并行度。

3. **fff_trigger 的 key 不含 timestamp**——一次触发就一行,同 uuid 的重传消息直接覆盖(语义正确:同一次触发不需要两条)。**这个"不含 timestamp"的差别决定了它的行数天然就是 uuid 数,汇总统计不需要去重**——fff 汇总表从一开始就是对的。

4. **fdr/fcl 的 key 含 timestamp_utc**——因为它们是**状态快照流**:同一个 uuid 每次状态变更(waiting→dumping→success、重试)都是一条独立消息,各带自己的时间戳。含 timestamp 才能保留完整状态轨迹(耗时可算、重试历史可查、乱序消息不会互相覆盖)。**代价:同 uuid 多行,fdr 2.08 行/uuid、fcl 4.03 行/uuid,行数≠事件数**——这是后面所有坑的总根源。

5. **monitor_analysis 的 key 不含 timestamp,含 uuid**——它是观测层:每个 uuid 一行,三阶段状态写在同一列组里。终态消息持续更新(UNIQUE 覆盖)。**它是链路统计的权威表**。

### fdr/fcl 的 key 设计专题:为什么含 timestamp、迟到的数据为什么不覆盖、以及"要覆盖且终态不变"的 sequence 方案

**① 为什么 fdr/fcl 的 UNIQUE KEY 要带上 timestamp_utc**

fdr/fcl 与 fff_trigger 的本质区别:fff_trigger 的消息是**单事件**(一次触发就一条消息),而 fdr/fcl 的消息是**状态变更事件流**——车端落盘/上传是一个持续过程:

```
08:12  waiting   ← 开始等待
08:13  dumping   ← 落盘中(或第 1 次上传尝试)
08:20  success   ← 完成(或第 3 次重试成功)
09:40  waiting   ← 迟到回流的旧快照(车辆离线积压,重新上线补传)
```

每次变更都是**一条独立消息、各带自己的事件时间戳**。key 含 timestamp 的三个理由:

1. **保留完整状态轨迹**:耗时可算(相邻快照的时间差)、重试历史可查(fcl 每 uuid 平均 4 行 = 4 次尝试)、排障时能看到"这个包卡在哪一步"。若 key 去掉 timestamp(只按 uuid),后到的消息会覆盖先到的——**历史轨迹全部丢失**。
2. **同状态重发不覆盖**:同 uuid 的同状态消息(车端对 waiting 的重复上报,时间戳不同)各自成行,不互相吞。
3. **与"事件流"的消费语义一致**:Kafka 每条消息就是一个快照,明细表如实落,不在写入时做任何"合并判断"——把语义判断留给读取/聚合层,写入路径保持最简(每秒数万条消息的吞吐优先)。

**② 迟到上传的数据,为什么入库时不判断时间戳、不覆盖同 uuid 的旧行**

即"为什么不用 UNIQUE(uuid)+timestamp 覆盖"在写入时就把表压缩成每 uuid 一行。四个原因:

1. **key 里有 timestamp,根本触发不了覆盖**——(dt, uuid, event, anon, ts) 五元组下,迟到行的 ts 与已存行不同,key 不同 → 永远是 INSERT,不是 UPDATE。覆盖从机制上就不存在。
2. **车端没有可靠的"事件版本号"**——时间戳是消息自己的,车端离线补传时**无法保证按时间顺序发送**(积压消息可能乱序 flush)。如果按"到达顺序覆盖"(UNIQUE 默认行为),一条**迟到的 waiting 会把已存 success 覆盖回 waiting**——数据事实被破坏。
3. **按时间戳判断覆盖需要读旧值,写入路径不可承受**——每条消息写入前要先查同 uuid 已存行的 timestamp 比较,亿级/天的写入量下这是把随机读塞进写路径,吞吐崩塌。列存引擎的写入吞吐正是靠"不读旧值、只追加"换来的。
4. **业务要求全量保留**——排障需要"这个 uuid 从 waiting 到 success 的每一步"和"重试了几次、每次为什么失败"。任何写入侧的压缩都是不可逆的信息丢弃。

**③ 如果要"每 uuid 一行 + 覆盖"且保证终态不被迟到数据破坏:sequence column 方案**

Doris 对 UNIQUE 表提供 sequence column 机制——**同 key 冲突时,只有 sequence 值更大的行才覆盖**,从机制上解决"迟到乱序覆盖":

```sql
CREATE TABLE fdr_trigger_v2 (
    dt DATE NOT NULL,
    uuid VARCHAR(64) NOT NULL,
    event_name VARCHAR(128), anonymous_id VARCHAR(128),
    status VARCHAR(32), detail VARCHAR(256),
    timestamp_utc DATETIME(6),
    -- 复合序:状态优先级 × 时间基准,一个 BIGINT 同时编码两级比较
    seq BIGINT
) UNIQUE KEY(dt, uuid)                          -- timestamp 移出 key,每 uuid 一行
DISTRIBUTED BY HASH(anonymous_id) BUCKETS 128
PROPERTIES (
    "function_column.sequence_col" = "seq",
    "enable_unique_key_merge_on_write" = "true"
);
-- ETL 写入前:seq = status_priority × 10^11 + unix_timestamp(timestamp_utc)
-- 终态(success/discard/fail)=2,中间态(waiting/dumping)=1
```

**仲裁规则**(复合序为什么这样设计):

| 场景 | seq 比较 | 结果 |
|---|---|---|
| 新 success 覆盖旧 waiting | 终态(2)>中间态(1) | ✓ 覆盖,正常推进 |
| **迟到的 waiting(即使时间戳更新)打已存 success** | 1<2 | ✓ **拒绝覆盖,终态受保护** |
| 迟到的旧快照(ts 更小) | 同状态比 ts | ✓ 丢弃 |
| discard 重试刷新(同为终态,ts 更新) | 2>2 比 ts | ✓ 覆盖,取最新终态 |

单纯用 timestamp 做 sequence 有个死角:迟到的**新时间戳中间态**(重试触发的新 waiting,ts 比已存 success 大)会覆盖终态。**状态优先级放在时间戳高位**正是堵这个死角——"终态一旦写入,中间态永远覆盖不了它"。

**该方案的收益与代价**(已评估,当前决策是不采用):

- 收益:行数砍半(存储/compaction/查询扫描全部减半)、COUNT DISTINCT 简化回 COUNT、统计口径天然一行一事件
- 代价:①**中间态历史全丢**(重试轨迹/耗时计算不可回溯)——与"全量保留"的业务要求冲突,这是否决主因;②表重建 + 双写迁移工程;③写入端所有路径必须带 seq 值,否则拒写
- **结论:明细层维持全量保留的快照流,把"压成一行"的语义放到聚合层实现**——聚合用 `COUNT(DISTINCT CASE WHEN status=... THEN uuid END)`("出现过终态"≡"压缩后留终态",统计结果与压缩方案完全一致,但历史一行不丢)

---

## 三、聚合明细表踩到的坑(按发现顺序,全部实测)

### 坑 1:按行聚合状态快照流 → 计数虚高 2~4 倍

fdr/fcl 汇总表的 `event_count = COUNT(*)`,而明细一行是一个状态快照不是一次事件:
```
fdr:行数 5200 万/天 vs uuid 2497 万/天 → event_count 虚高 107%
fcl:行数 7649 万/天 vs uuid 1897 万/天 → 虚高约 4 倍
```
后果:落盘成功率被系统性腰斩(分母虚高一倍,分子 success 恰好接近 uuid 数)。**看板显示 20.7%,真实约 60%**。

### 坑 2:fdr 状态消息丢失 → success 低估 25%

fdr 和 fcl 是两条独立消费链路,fdr 的终态消息每天约 300 万个 uuid 丢失(数据实际已落盘并上传,但 fdr 状态永远停在 waiting/dumping)。证据:ads 表里"上传中但 fdr_status=waiting"的 uuid 有 396 万/天。
后果:fdr 明细/汇总的 success_count 系统性低估;上游 funnel 因此用 `fdr_status='success' OR fcl_status!=''` 兜底(以结果为准),这个 OR 口径反而更接近真值。

### 坑 3:迟到回流 → "取最后一条"口径全灭

车端离线积压的消息在车辆重新上线后补传,**主体是中间态快照**(中间态:终态回流比例约 28:1)。迟到的快照行会**堆在终态行之后**:
- 8/26 分区几小时内"最后一条是中间态"的占比从 9% 涨到 77%
- `ROW_NUMBER() OVER (PARTITION BY uuid ORDER BY timestamp DESC) = 1` 取"最后一条"当终态 → 被迟到快照骗,且不幂等(每天重算都在漂)
- 正确姿势:**"出现过终态值"**(`COUNT(DISTINCT CASE WHEN status='success' THEN uuid END)`)——集合成员关系不因迟到行改变,对回流天然免疫
- 实测真死账(从未有过任何终态)只有 3.5%(历史)~9.1%(近 3 天,还在等回流),不是 75%

### 坑 4:vehicle_count 不可跨行 SUM

去重车辆数是"分组内去重",跨行相加会把同一辆车按事件数×版本数×天数重复计(实测膨胀 27~85 倍)。跨维度用车数必须查 `_agg` 表(按天+维度预去重,`__ALL__` 汇总行)。

### 坑 5:`__ALL__` 哨兵的双向规则

汇总表同时存 `__ALL__` 汇总行和真实事件行:查**总量**必须只查 `__ALL__` 行(否则重复计车/计数翻倍);查**分布**必须排除 `__ALL__` 行。漏了哪边数字翻倍,曾出过 760 万车辆数(真实 28.6 万)的事故。

### 坑 6:两套 tag 命名并存

funnel 数据源(ads_do_cfdi_daily)存**白话原文**(`Exceeds the maximum number of files`、`query cloud DISCARD, detail:...`),fdr/fcl 汇总表存**规范名**(`max_files_exceeded`、`event_in_blacklog`)。前端剔除正常丢弃时用错一套 → 匹配全落空,数字纹丝不动地错。教训:**每处匹配常量必须注明自己对应哪张表的命名**。

### 坑 7:子查询复用 where 的占位符陷阱

同一条 SQL 里主查询和子查询复用同一个 where 字符串时,占位符 `?` 数量翻倍,但参数只传一份 → SQL 直接报错(曾导致 P95 全列显示"—")。规则:**几个子查询复用 where,参数就复制几份,顺序与占位符出现顺序一致**。

---

## 四、聚合表(汇总表)的 key 设计思路

### 模型选择:AGGREGATE KEY + REPLACE_IF_NOT_NULL(不是 UNIQUE)

```sql
-- 例:ads_cfdi_vehicle_daily_summary_agg
AGGREGATE KEY(`dt`, `summary_key`)     -- summary_key = sha256(全部业务维度组合)
数值列 REPLACE_IF_NOT_NULL
DISTRIBUTED BY HASH(summary_key) BUCKETS 8
PARTITION BY RANGE(dt)                 -- 按天分区,动态分区保留 -360 天
```

**为什么不是 UNIQUE**:聚合表要的是"算好终值写入、重复算就覆盖"的幂等。AGGREGATE + REPLACE_IF_NOT_NULL 提供的语义:
- 同 key 新值覆盖旧值(重跑任务不翻倍——若用 SUM 聚合类型,重跑会把结果再加一遍)
- **NULL 不覆盖旧值**(残留上一轮的旧数字,这是"数字不回退"的来源——重刷时要么保证列非空,要么先 DELETE 分区再写)

### 聚合的两级架构(累加发生在哪)

```
明细表(亿级/天)
  │  ETL 凌晨批任务:WHERE dt=昨天 GROUP BY 维度
  │  ← 累加/去重在这一步完成(SUM/COUNT DISTINCT),每天全量重算当天
  ▼
汇总表(千~万行/天)
  │  写入的是"当天终值",每 (dt, 维度组合) 一行
  │  同 key 重算 → REPLACE 覆盖(幂等)
  ▼
看板查询(毫秒级,永远查聚合好的结果)
```

**幂等的实现是"每次全量重算 + 覆盖写入",不是"只处理增量"**——凌晨跑一次和中午补跑一次,第二次也是全量重算当天,终值直接覆盖凌晨的中间值。

### summary_key = sha256(维度组合)

- key 稳定性是命门:维度清单一旦变化(增删维度),新旧 key 不同 → 旧行残留 + 新行并存,**行数翻倍、SUM 翻倍**。改口径必须先清旧行
- 用 hash 而不是拼维度串:定长 64 字节,不随维度字符串长度膨胀

### 三个口径层级的分工(最终定稿)

| 层 | 表 | 职责 |
|---|---|---|
| 明细层 | fdr/fcl_trigger | 状态全量保留(重试历史/耗时轨迹/排障),**统计不做基数来源** |
| 观测层 | monitor_analysis → ads_do_cfdi_daily | uuid 一行的链路终态(权威计数源),`fdr=success OR fcl≠''` 兜状态丢失 |
| 聚合层 | *_daily_summary | 维度切片 + P95 类加权指标;**计数列改 uuid 去重口径**(ETL 侧 COUNT DISTINCT CASE,对回流免疫) |

---

## 五、一页速查(把四百行的排查压成十行)

1. uuid = 这辆车这一次触发;anonymous_id = 车;分桶都按车
2. fff_trigger 的 key 不含 timestamp → 天然 uuid 一行,统计不踩坑
3. fdr/fcl 的 key 含 timestamp → 状态快照流,行数 ≠ 事件数(2~4 倍)
4. 数事件数:`COUNT(DISTINCT CASE WHEN status=... THEN uuid END)`,永远不要 ROW_NUMBER 取最后一条(迟到回流会骗它)
5. 车辆数:只信 `_agg` 表的 `__ALL__` 行,明细表去重跨不了维度
6. 落盘成功的权威定义:`fdr_status='success' OR fcl_status!=''`(以结果为准,兜状态丢消息)
7. 上传入口有三个:FFF 正路 + Forever_log 旁路(不落盘)+ FDC
8. 聚合表幂等 = 全量重算 + REPLACE 覆盖;NULL 不覆盖是残留坑
9. tag 白话名(ads 表)和规范名(汇总表)是两套,匹配常量必须注明出处
10. 子查询复用 where,参数按占位符出现顺序复制对应份数


---

## 六、附录:核心表建表语句(生产库导出,分区列表已折叠)

### dwd_cfdi_basic_fff_trigger

> 设计要点:key 不含 timestamp → 一次触发一行,天然 uuid 粒度,统计无需去重

```sql
CREATE TABLE `dwd_cfdi_basic_fff_trigger` (
  `dt` date NOT NULL COMMENT '分区日期',
  `uuid` varchar(64) NOT NULL COMMENT 'UUID唯一标识',
  `event_name` varchar(128) NOT NULL COMMENT '事件名称',
  `anonymous_id` varchar(128) NOT NULL COMMENT '匿名ID(车牌号)',
  `timestamp_utc` datetime(6) NOT NULL COMMENT 'UTC时间戳(微秒)',
  `create_at` date NULL COMMENT '创建日期',
  `trigger_time` bigint NULL COMMENT '触发时间戳',
  `utc_diff_us` bigint NULL COMMENT 'UTC时间差(微秒)',
  `before` tinyint NULL COMMENT '触发前秒数',
  `after` tinyint NULL COMMENT '触发后秒数',
  `filter_name` varchar(128) NULL COMMENT '过滤器名称',
  `trigger_type` varchar(64) NULL COMMENT '触发类型',
  `collect_type` varchar(64) NULL COMMENT '采集类型',
  `status` varchar(64) NULL COMMENT '状态',
  `on_autopilot` varchar(64) NULL COMMENT '自动驾驶状态',
  `function_mode` varchar(64) NULL COMMENT '功能模式',
  `sw_version` varchar(512) NULL COMMENT '软件版本',
  `project_name` varchar(64) NULL COMMENT '项目名称',
  `car_type` varchar(64) NULL COMMENT '车型',
  `vehicle_source` varchar(64) NULL COMMENT '车辆来源',
  `bj02_lat` text NULL COMMENT '北京02坐标-纬度',
  `bj02_lon` text NULL COMMENT '北京02坐标-经度',
  `road_type` text NULL COMMENT '道路类型',
  `fdi_project_name` varchar(128) NULL COMMENT 'FDI项目名称',
  `project_car_type` varchar(128) NULL COMMENT '项目车型',
  `vehicle_source_cn` varchar(64) NULL COMMENT '车辆来源中文',
  `tags` array<varchar(256)> NULL COMMENT '标签数组',
  `detail` text NULL COMMENT '详细信息',
  INDEX idx_anonymous_id (`anonymous_id`) USING INVERTED,
  INDEX idx_timestamp_utc (`timestamp_utc`) USING INVERTED,
  INDEX idx_event_name (`event_name`) USING INVERTED
) ENGINE=OLAP
UNIQUE KEY(`dt`, `uuid`, `event_name`, `anonymous_id`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 135 个,首:p20260522 尾:p20261003)...
  PARTITION p20261004 VALUES [('2026-10-04'), ('2026-10-05')))
DISTRIBUTED BY HASH(`anonymous_id`) BUCKETS 128
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-100",
"dynamic_partition.end" = "35",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "128",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"enable_unique_key_merge_on_write" = "true",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728",
"enable_mow_light_delete" = "false"
);;
```

### dwd_cfdi_basic_fff_running

> 设计要点:心跳快照流:key 含 timestamp 保留每次心跳(每 5 分钟一跳)

```sql
CREATE TABLE `dwd_cfdi_basic_fff_running` (
  `dt` date NOT NULL COMMENT '分区日期',
  `filter_name` varchar(256) NOT NULL COMMENT '过滤器名称',
  `anonymous_id` varchar(256) NOT NULL COMMENT '匿名ID(车牌号)',
  `timestamp_utc` datetime(6) NOT NULL COMMENT 'UTC时间戳(微秒)',
  `create_at` date NULL COMMENT '创建日期',
  `collect_type` varchar(256) NULL COMMENT '采集类型',
  `sw_version` varchar(512) NULL COMMENT '软件版本',
  `project_name` varchar(64) NULL COMMENT '项目名称',
  `car_type` varchar(64) NULL COMMENT '车型',
  `vehicle_source` varchar(64) NULL COMMENT '车辆来源',
  `switch_on` int NULL COMMENT '开关状态(0/1)',
  `version` varchar(256) NULL COMMENT '过滤器版本',
  `on_autopilot` varchar(64) NULL COMMENT '自动驾驶状态',
  `function_mode` varchar(64) NULL COMMENT '功能模式',
  `status` text NULL COMMENT '状态(JSON)',
  `fdi_project_name` varchar(128) NULL COMMENT 'FDI项目名称',
  `project_car_type` varchar(128) NULL COMMENT '项目车型',
  `vehicle_source_cn` varchar(64) NULL COMMENT '车辆来源中文',
  INDEX idx_anonymous_id (`anonymous_id`) USING INVERTED,
  INDEX idx_timestamp_utc (`timestamp_utc`) USING INVERTED
) ENGINE=OLAP
UNIQUE KEY(`dt`, `filter_name`, `anonymous_id`, `timestamp_utc`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 135 个,首:p20260522 尾:p20261003)...
  PARTITION p20261004 VALUES [('2026-10-04'), ('2026-10-05')))
DISTRIBUTED BY HASH(`anonymous_id`) BUCKETS 32
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-100",
"dynamic_partition.end" = "35",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "32",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"enable_unique_key_merge_on_write" = "true",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728",
"enable_mow_light_delete" = "false"
);;
```

### dwd_cfdi_basic_fff_close

> 设计要点:关闭事件:维度列全进 key,同维度组合覆盖

```sql
CREATE TABLE `dwd_cfdi_basic_fff_close` (
  `dt` date NOT NULL COMMENT '分区日期',
  `filter_name` varchar(128) NOT NULL COMMENT '过滤器名称',
  `version` varchar(256) NOT NULL COMMENT '过滤器版本',
  `reason` varchar(4500) NOT NULL COMMENT '关闭原因',
  `anonymous_id` varchar(64) NOT NULL COMMENT '车牌号',
  `create_at` date NOT NULL COMMENT '创建日期',
  `sw_version` varchar(256) NULL COMMENT '软件版本',
  `timestamp_utc` datetime(6) NULL COMMENT '时间戳(UTC)',
  `project_name` varchar(128) NULL COMMENT '项目名称',
  `car_type` varchar(64) NULL COMMENT '车型',
  `vehicle_source` varchar(32) NULL COMMENT '车辆来源',
  `fdi_project_name` varchar(128) NULL COMMENT 'FDI项目名称',
  `project_car_type` varchar(256) NULL COMMENT '项目车型',
  `vehicle_source_cn` varchar(64) NULL COMMENT '车辆来源中文',
  INDEX idx_anonymous_id (`anonymous_id`) USING INVERTED,
  INDEX idx_timestamp_utc (`timestamp_utc`) USING INVERTED
) ENGINE=OLAP
UNIQUE KEY(`dt`, `filter_name`, `version`, `reason`, `anonymous_id`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 183 个,首:p20260303 尾:p20260901)...
  PARTITION p20260902 VALUES [('2026-09-02'), ('2026-09-03')))
DISTRIBUTED BY HASH(`anonymous_id`) BUCKETS AUTO
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-180",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "10",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"enable_unique_key_merge_on_write" = "true",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728",
"enable_mow_light_delete" = "false"
);;
```

### dwd_basic_fdr_trigger

> 设计要点:状态快照流:key 含 timestamp_utc,同 uuid 每个状态一行(行数≈uuid 的 2.08 倍,是计数坑的总根源)

```sql
CREATE TABLE `dwd_basic_fdr_trigger` (
  `dt` date NOT NULL COMMENT '分区日期',
  `uuid` varchar(256) NOT NULL COMMENT 'UUID唯一标识',
  `event_name` varchar(256) NOT NULL COMMENT '事件名称',
  `anonymous_id` varchar(256) NOT NULL COMMENT '匿名ID(车牌号)',
  `timestamp_utc` datetime(6) NULL COMMENT 'UTC时间戳(微秒)',
  `create_at` date NULL COMMENT '创建日期',
  `sw_version` varchar(512) NULL COMMENT '软件版本',
  `dse` varchar(256) NULL COMMENT 'DSE信息',
  `td_mb` varchar(64) NULL COMMENT 'TD大小(MB)',
  `tm_mb` varchar(64) NULL COMMENT 'TM大小(MB)',
  `trigger_timestamp` bigint NULL COMMENT '触发时间戳',
  `begin_timestamp_uts` bigint NULL COMMENT '开始时间戳(微秒)',
  `end_timestamp_uts` bigint NULL COMMENT '结束时间戳(微秒)',
  `dump_timestamp` bigint NULL COMMENT 'Dump时间戳',
  `status` varchar(64) NULL COMMENT '状态',
  `detail` text NULL COMMENT '详细信息',
  `time_cost_ms` bigint NULL COMMENT '耗时(毫秒)',
  `type` varchar(64) NULL COMMENT '类型',
  `project_name` varchar(64) NULL COMMENT '项目名称',
  `car_type` varchar(64) NULL COMMENT '车型',
  `vehicle_source` varchar(64) NULL COMMENT '车辆来源',
  `fdi_project_name` varchar(128) NULL COMMENT 'FDI项目名称',
  `project_car_type` varchar(128) NULL COMMENT '项目车型',
  `vehicle_source_cn` varchar(64) NULL COMMENT '车辆来源中文',
  INDEX idx_anonymous_id (`anonymous_id`) USING INVERTED,
  INDEX idx_event_name (`event_name`) USING INVERTED,
  INDEX idx_timestamp_utc (`timestamp_utc`) USING INVERTED
) ENGINE=OLAP
UNIQUE KEY(`dt`, `uuid`, `event_name`, `anonymous_id`, `timestamp_utc`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 131 个,首:p20260526 尾:p20261003)...
  PARTITION p20261004 VALUES [('2026-10-04'), ('2026-10-05')))
DISTRIBUTED BY HASH(`anonymous_id`) BUCKETS 128
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"bloom_filter_columns" = "anonymous_id, event_name, uuid",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-96",
"dynamic_partition.end" = "35",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "128",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"enable_unique_key_merge_on_write" = "true",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728",
"enable_mow_light_delete" = "false"
);;
```

### dwd_cfdi_basic_fcl_trigger

> 设计要点:状态快照流:同 fdr,重试更多(行数≈uuid 的 4 倍),trigger_source 区分 FFF/Forever_log/FDC 入口

```sql
CREATE TABLE `dwd_cfdi_basic_fcl_trigger` (
  `dt` date NOT NULL COMMENT '分区日期',
  `uuid` varchar(256) NOT NULL COMMENT 'UUID唯一标识',
  `event_name` varchar(256) NOT NULL COMMENT '事件名称',
  `anonymous_id` varchar(256) NOT NULL COMMENT '匿名ID(车牌号)',
  `timestamp_utc` datetime(6) NULL COMMENT 'UTC时间戳(微秒)',
  `create_at` date NULL COMMENT '创建日期',
  `status` varchar(64) NULL COMMENT '状态',
  `detail` text NULL COMMENT '详细信息',
  `complete_percent` bigint NULL COMMENT '完成百分比',
  `local_file` varchar(512) NULL COMMENT '本地文件路径',
  `upload_fail_times` bigint NULL COMMENT '上传失败次数',
  `prefix_stitch` varchar(256) NULL COMMENT '前缀拼接',
  `trigger_source` varchar(64) NULL COMMENT '触发来源(FFF/Forever_log/FDC)',
  `sw_version` varchar(512) NULL COMMENT '软件版本',
  `project_name` varchar(64) NULL COMMENT '项目名称',
  `car_type` varchar(64) NULL COMMENT '车型',
  `vehicle_source` varchar(64) NULL COMMENT '车辆来源',
  `fdi_project_name` varchar(128) NULL COMMENT 'FDI项目名称',
  `project_car_type` varchar(128) NULL COMMENT '项目车型',
  `vehicle_source_cn` varchar(64) NULL COMMENT '车辆来源中文',
  INDEX idx_anonymous_id (`anonymous_id`) USING INVERTED,
  INDEX idx_event_name (`event_name`) USING INVERTED,
  INDEX idx_timestamp_utc (`timestamp_utc`) USING INVERTED
) ENGINE=OLAP
UNIQUE KEY(`dt`, `uuid`, `event_name`, `anonymous_id`, `timestamp_utc`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 99 个,首:p20260526 尾:p20260901)...
  PARTITION p20260902 VALUES [('2026-09-02'), ('2026-09-03')))
DISTRIBUTED BY HASH(`anonymous_id`) BUCKETS 128
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"bloom_filter_columns" = "anonymous_id, event_name, uuid",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-96",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "128",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"enable_unique_key_merge_on_write" = "true",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728",
"enable_mow_light_delete" = "false"
);;
```

### dwd_cfdi_basic_fcl_uploadinfo

> 设计要点:包级一行(不含 timestamp),上传成功的最终记录

```sql
CREATE TABLE `dwd_cfdi_basic_fcl_uploadinfo` (
  `dt` date NOT NULL COMMENT '分区日期',
  `uuid` varchar(128) NOT NULL COMMENT 'UUID',
  `event_name` varchar(128) NOT NULL COMMENT '事件名称',
  `anonymous_id` varchar(64) NOT NULL COMMENT '车牌号',
  `create_at` date NOT NULL COMMENT '创建日期',
  `prefix_stitch` varchar(256) NULL COMMENT '前缀拼接',
  `trigger_source` varchar(64) NULL COMMENT '触发来源(FFF、Forever_log、FDC)',
  `package_size` bigint NULL COMMENT '包大小(字节)',
  `begin_at` bigint NULL COMMENT '上传开始时间戳',
  `complete_at` bigint NULL COMMENT '上传完成时间戳',
  `total_cost` bigint NULL COMMENT '总耗时(毫秒)',
  `sw_version` varchar(256) NULL COMMENT '软件版本',
  `timestamp_utc` datetime(6) NULL COMMENT '时间戳(UTC)',
  `project_name` varchar(128) NULL COMMENT '项目名称',
  `car_type` varchar(64) NULL COMMENT '车型',
  `vehicle_source` varchar(32) NULL COMMENT '车辆来源',
  `fdi_project_name` varchar(128) NULL COMMENT 'FDI项目名称',
  `project_car_type` varchar(256) NULL COMMENT '项目车型',
  `vehicle_source_cn` varchar(64) NULL COMMENT '车辆来源中文',
  INDEX idx_anonymous_id (`anonymous_id`) USING INVERTED,
  INDEX idx_timestamp_utc (`timestamp_utc`) USING INVERTED,
  INDEX idx_event_name (`event_name`) USING INVERTED
) ENGINE=OLAP
UNIQUE KEY(`dt`, `uuid`, `event_name`, `anonymous_id`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 183 个,首:p20260303 尾:p20260901)...
  PARTITION p20260902 VALUES [('2026-09-02'), ('2026-09-03')))
DISTRIBUTED BY HASH(`uuid`) BUCKETS AUTO
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-180",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "10",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"enable_unique_key_merge_on_write" = "true",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728",
"enable_mow_light_delete" = "false"
);;
```

### dwd_cfdi_status_monitor_analysis

> 设计要点:链路权威表:每 uuid 一行,三阶段状态同行,终态消息持续覆盖更新

```sql
CREATE TABLE `dwd_cfdi_status_monitor_analysis` (
  `dt` date NOT NULL COMMENT '日期',
  `anonymous_id` varchar(256) NOT NULL COMMENT '匿名ID(车牌号)',
  `event_name` varchar(256) NOT NULL COMMENT '事件名称',
  `uuid` varchar(256) NOT NULL COMMENT 'UUID唯一标识',
  `create_at` date NULL,
  `filter_name` varchar(256) NULL COMMENT '过滤器名称(FFF)',
  `fff_sw_version` varchar(512) NULL COMMENT 'FFF软件版本',
  `fdr_sw_version` varchar(512) NULL COMMENT 'FDR软件版本',
  `fcl_sw_version` varchar(512) NULL COMMENT 'FCL软件版本',
  `trigger_type` varchar(64) NULL COMMENT '触发类型(FFF)',
  `collect_type` varchar(64) NULL COMMENT '采集类型(FFF)',
  `fff_updated_at` bigint NULL COMMENT 'FFF时间戳UTC(微秒)',
  `fdr_updated_at` bigint NULL COMMENT 'FDR时间戳UTC(微秒)',
  `fcl_updated_at` bigint NULL COMMENT 'FCL时间戳UTC(微秒)',
  `fff_status` varchar(64) NULL COMMENT 'FFF状态',
  `fdr_status` varchar(64) NULL COMMENT 'FDR状态',
  `fcl_status` varchar(64) NULL COMMENT 'FCL状态',
  `fff_detail` text NULL COMMENT 'FFF详情',
  `fdr_detail` text NULL COMMENT 'FDR详情',
  `fcl_detail` text NULL COMMENT 'FCL详情',
  `begin_timestamp_uts` bigint NULL COMMENT '开始时间戳',
  `dump_timestamp` bigint NULL COMMENT 'Dump时间戳',
  `end_timestamp_uts` bigint NULL COMMENT '结束时间戳',
  `md5` varchar(256) NULL COMMENT 'MD5哈希',
  `bag_name` varchar(512) NULL COMMENT '包名',
  `complete_percent` bigint NULL COMMENT '完成百分比',
  `project_name` varchar(64) NULL COMMENT '项目名称(team)',
  `car_type` varchar(64) NULL COMMENT '车型',
  `vehicle_source` varchar(64) NULL COMMENT '车辆来源',
  `timestamp_utc` bigint NULL COMMENT '通用时间戳UTC(微秒)',
  `fdi_project_name` varchar(128) NULL COMMENT 'FDI项目名称',
  `project_car_type` varchar(128) NULL COMMENT '项目车型',
  `vehicle_source_cn` varchar(64) NULL COMMENT '车辆来源中文',
  `dse` varchar(256) NULL,
  INDEX idx_vehicle_source (`vehicle_source`) USING INVERTED COMMENT '车辆来源索引',
  INDEX idx_project_name (`project_name`) USING INVERTED COMMENT '项目名称索引',
  INDEX idx_car_type (`car_type`) USING INVERTED COMMENT '车型索引',
  INDEX idx_fff_sw_version (`fff_sw_version`) USING INVERTED COMMENT 'FFF软件版本索引',
  INDEX idx_fff_status (`fff_status`) USING INVERTED COMMENT 'FFF状态索引',
  INDEX idx_fdr_status (`fdr_status`) USING INVERTED COMMENT 'FDR状态索引',
  INDEX idx_fcl_status (`fcl_status`) USING INVERTED COMMENT 'FCL状态索引'
) ENGINE=OLAP
UNIQUE KEY(`dt`, `anonymous_id`, `event_name`, `uuid`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 215 个,首:p20260303 尾:p20261003)...
  PARTITION p20261004 VALUES [('2026-10-04'), ('2026-10-05')))
DISTRIBUTED BY HASH(`anonymous_id`) BUCKETS 4
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-180",
"dynamic_partition.end" = "35",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "4",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"enable_unique_key_merge_on_write" = "true",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728",
"enable_mow_light_delete" = "false"
);;
```

### dwd_basic_fdr_trigger_daily_summary

> 设计要点:AGGREGATE+REPLACE_IF_NOT_NULL:summary_key=sha256(维度),重算覆盖幂等,NULL 不覆盖(残留坑)

```sql
CREATE TABLE `dwd_basic_fdr_trigger_daily_summary` (
  `dt` date NOT NULL COMMENT '统计日期',
  `summary_key` varchar(64) NOT NULL COMMENT '业务唯一键: sha256(本表全部业务维度)',
  `summary_grain` varchar(32) NOT NULL DEFAULT "overview" COMMENT '统计粒度: overview/status/reason',
  `event_name` varchar(256) NOT NULL DEFAULT "__ALL__" COMMENT '事件名; 全事件汇总为 __ALL__',
  `status_tag` varchar(64) NOT NULL DEFAULT "__ALL__" COMMENT 'FDR 状态标签',
  `detail_tag` varchar(128) NOT NULL DEFAULT "__ALL__" COMMENT 'FDR 失败原因标签',
  `sw_version` varchar(512) NOT NULL DEFAULT "" COMMENT '智驾版本',
  `type` varchar(64) NOT NULL DEFAULT "" COMMENT '触发类型',
  `dse` varchar(256) NOT NULL DEFAULT "" COMMENT 'DSE',
  `project_name` varchar(128) NOT NULL DEFAULT "" COMMENT '项目',
  `fdi_project_name` varchar(128) NOT NULL DEFAULT "" COMMENT 'FDI 项目',
  `car_type` varchar(64) NOT NULL DEFAULT "" COMMENT '车型',
  `project_car_type` varchar(256) NOT NULL DEFAULT "" COMMENT '项目车型',
  `vehicle_source` varchar(64) NOT NULL DEFAULT "" COMMENT '车辆来源',
  `vehicle_source_cn` varchar(64) NOT NULL DEFAULT "" COMMENT '车辆来源中文',
  `event_count` bigint NOT NULL DEFAULT "0" COMMENT 'FDR 记录数',
  `vehicle_count` bigint NOT NULL DEFAULT "0" COMMENT '当前完整分组内去重车辆数',
  `success_count` bigint NOT NULL DEFAULT "0" COMMENT '落盘成功次数',
  `failed_count` bigint NOT NULL DEFAULT "0" COMMENT '落盘失败次数',
  `td_mb_count` bigint NOT NULL DEFAULT "0" COMMENT 'TD 磁盘大小有效样本数',
  `td_mb_sum` double NOT NULL DEFAULT "0" COMMENT 'TD 磁盘大小总和, MB',
  `td_mb_avg` double NULL COMMENT 'TD 单包磁盘大小均值, MB',
  `td_mb_p95` double NULL COMMENT 'TD 单包磁盘大小 P95, MB',
  `td_mb_max` double NULL COMMENT 'TD 单包磁盘大小最大值, MB',
  `tm_mb_count` bigint NOT NULL DEFAULT "0" COMMENT 'TM 内存大小有效样本数',
  `tm_mb_sum` double NOT NULL DEFAULT "0" COMMENT 'TM 内存大小总和, MB',
  `tm_mb_avg` double NULL COMMENT 'TM 单包内存大小均值, MB',
  `tm_mb_p95` double NULL COMMENT 'TM 单包内存大小 P95, MB',
  `tm_mb_max` double NULL COMMENT 'TM 单包内存大小最大值, MB',
  `time_cost_ms_count` bigint NOT NULL DEFAULT "0" COMMENT '落盘耗时有效样本数',
  `time_cost_ms_sum` bigint NOT NULL DEFAULT "0" COMMENT '落盘耗时总和, ms',
  `time_cost_ms_avg` double NULL COMMENT '落盘耗时均值, ms',
  `time_cost_ms_p95` double NULL COMMENT '落盘耗时 P95, ms',
  `time_cost_ms_max` bigint NULL COMMENT '落盘耗时最大值, ms',
  `td_tm_parse_failed_count` bigint NOT NULL DEFAULT "0" COMMENT 'TD/TM 数值转换失败记录数',
  `summary_created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '汇总创建时间',
  `summary_updated_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '汇总更新时间'
) ENGINE=OLAP
UNIQUE KEY(`dt`, `summary_key`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 363 个,首:p20250904 尾:p20260901)...
  PARTITION p20260902 VALUES [('2026-09-02'), ('2026-09-03')))
DISTRIBUTED BY HASH(`summary_key`) BUCKETS 16
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-360",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "16",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"enable_unique_key_merge_on_write" = "true",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728",
"enable_mow_light_delete" = "false"
);;
```

### ads_cfdi_vehicle_daily_summary_agg

> 设计要点:车辆去重专用:_agg 表,__ALL__ 行是跨维度唯一正确口径

```sql
CREATE TABLE `ads_cfdi_vehicle_daily_summary_agg` (
  `dt` date NOT NULL COMMENT '统计日期',
  `summary_key` varchar(64) NOT NULL COMMENT '业务唯一键: sha256(事件/版本/项目/车型/车辆来源等低维维度)',
  `event_name` varchar(256) REPLACE_IF_NOT_NULL NULL COMMENT '事件名; 全事件汇总为 __ALL__',
  `sw_version` varchar(512) REPLACE_IF_NOT_NULL NULL COMMENT '智驾版本',
  `project_name` varchar(128) REPLACE_IF_NOT_NULL NULL COMMENT '项目',
  `fdi_project_name` varchar(128) REPLACE_IF_NOT_NULL NULL COMMENT 'FDI 项目',
  `car_type` varchar(64) REPLACE_IF_NOT_NULL NULL COMMENT '车型',
  `project_car_type` varchar(256) REPLACE_IF_NOT_NULL NULL COMMENT '项目车型',
  `vehicle_source` varchar(64) REPLACE_IF_NOT_NULL NULL COMMENT '车辆来源',
  `vehicle_source_cn` varchar(64) REPLACE_IF_NOT_NULL NULL COMMENT '车辆来源中文',
  `running_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FFF Running 去重车辆数',
  `running_switch_on_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FFF Running 且 switch_on=1 的去重车辆数',
  `trigger_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FFF Trigger 去重车辆数',
  `fff_success_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FFF 成功去重车辆数',
  `fff_failed_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FFF 失败去重车辆数',
  `fdr_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FDR 去重车辆数',
  `fdr_success_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FDR 成功去重车辆数',
  `fdr_failed_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FDR 失败去重车辆数',
  `fcl_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FCL 去重车辆数',
  `fcl_success_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FCL 成功去重车辆数',
  `fcl_failed_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'FCL 失败去重车辆数',
  `upload_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT 'UploadInfo 去重车辆数',
  `overall_success_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT '全链路成功去重车辆数; 只能由全链路口径任务写入',
  `overall_failed_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT '全链路失败去重车辆数; 只能由全链路口径任务写入',
  `overall_vehicle_count` bigint REPLACE_IF_NOT_NULL NULL COMMENT '全链路总去重车辆数',
  `summary_created_at` datetime REPLACE_IF_NOT_NULL NULL COMMENT '汇总创建时间',
  `summary_updated_at` datetime REPLACE_IF_NOT_NULL NULL COMMENT '汇总更新时间'
) ENGINE=OLAP
AGGREGATE KEY(`dt`, `summary_key`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 363 个,首:p20250904 尾:p20260901)...
  PARTITION p20260902 VALUES [('2026-09-02'), ('2026-09-03')))
DISTRIBUTED BY HASH(`summary_key`) BUCKETS 8
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-360",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "8",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728"
);;
```

### ads_do_cfdi_daily

> 设计要点:monitor_analysis 的 uuid 口径聚合(ETL:DELETE 当天分区重算),funnel 数据源,detail_tag 存白话原文

```sql
CREATE TABLE `ads_do_cfdi_daily` (
  `dt` date NOT NULL COMMENT '统计日期',
  `event_name` varchar(256) NOT NULL COMMENT '事件名称',
  `filter_name` varchar(256) NOT NULL COMMENT '筛选器名称（无则空串）',
  `car_type` varchar(64) NOT NULL COMMENT '车型',
  `project_name` varchar(64) NOT NULL COMMENT '项目名称',
  `fff_sw_version` varchar(128) NOT NULL COMMENT 'FFF 软件版本',
  `fff_status` varchar(32) NOT NULL COMMENT 'FFF 状态',
  `fdr_status` varchar(32) NOT NULL COMMENT 'FDR 状态',
  `fcl_status` varchar(32) NOT NULL COMMENT 'FCL 状态',
  `fff_detail_tag` varchar(64) NOT NULL COMMENT 'FFF 失败归类标签',
  `fdr_detail_tag` varchar(64) NOT NULL COMMENT 'FDR 失败归类标签',
  `fcl_detail_tag` varchar(64) NOT NULL COMMENT 'FCL 失败归类标签',
  `cnt` bigint SUM NULL DEFAULT "0" COMMENT '记录数'
) ENGINE=OLAP
AGGREGATE KEY(`dt`, `event_name`, `filter_name`, `car_type`, `project_name`, `fff_sw_version`, `fff_status`, `fdr_status`, `fcl_status`, `fff_detail_tag`, `fdr_detail_tag`, `fcl_detail_tag`)
PARTITION BY RANGE (`dt`)
(-- ...按天分区(动态分区自动建,此处折叠 183 个,首:p20260303 尾:p20260901)...
  PARTITION p20260902 VALUES [('2026-09-02'), ('2026-09-03')))
DISTRIBUTED BY HASH(`event_name`) BUCKETS 32
PROPERTIES (
"replication_allocation" = "tag.location.default: 2",
"min_load_replica_num" = "-1",
"is_being_synced" = "false",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.time_zone" = "Asia/Shanghai",
"dynamic_partition.start" = "-180",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "p",
"dynamic_partition.replication_allocation" = "tag.location.default: 2",
"dynamic_partition.buckets" = "32",
"dynamic_partition.create_history_partition" = "true",
"dynamic_partition.history_partition_num" = "-1",
"dynamic_partition.hot_partition_num" = "0",
"dynamic_partition.reserved_history_periods" = "NULL",
"dynamic_partition.storage_policy" = "",
"storage_medium" = "hdd",
"storage_format" = "V2",
"inverted_index_storage_format" = "V1",
"light_schema_change" = "true",
"disable_auto_compaction" = "false",
"enable_single_replica_compaction" = "false",
"group_commit_interval_ms" = "10000",
"group_commit_data_bytes" = "134217728"
);;
```
